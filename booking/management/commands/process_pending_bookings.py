from django.core.management.base import BaseCommand&#10;from django.utils import timezone&#10;from booking.tasks import update_scheduled_bookings&#10;&#10;class Command(BaseCommand):&#10;    help = 'Process all pending scheduled bookings where booking_time has passed'&#10;&#10;    def handle(self, *args, **options):&#10;        self.stdout.write('Processing pending bookings...')&#10;        &#10;        # Call the celery task&#10;        result = update_scheduled_bookings.delay()&#10;        self.stdout.write(&#10;            self.style.SUCCESS(f'Triggered update_scheduled_bookings task: {result.id}')&#10;        )&#10;        self.stdout.write(self.style.SUCCESS('Check Celery worker logs and dashboard.'))
